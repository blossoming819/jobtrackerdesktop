# JobTracker Desktop 与 ApplyMate 最终用户说明

> 适用版本：JobTracker Desktop 1.3.x / ApplyMate 0.1（Alpha）
>
> 当前支持：Windows 10 / 11；ApplyMate 通过 Chrome / Edge 开发者模式加载

## 一、产品介绍

JobTracker Desktop（安装后的名称为“个人求职助手”）是一款本地桌面客户端，用来集中管理求职过程中的岗位投递、招聘进度、简历文件、个人档案、面试记录、笔记资料和日程事项。

ApplyMate 是配套的浏览器填写助手。它扫描当前招聘页面，将页面字段与经过授权的本地档案匹配，先生成填写方案，再由用户确认后辅助写入。

**ApplyMate 不会点击最终提交按钮。申请内容必须由你复核，并由你本人提交。**

你可以用它记录：

- 投递了哪些公司和岗位
- 每个岗位当前进行到哪一步
- 每个岗位使用了哪份简历
- 每个岗位关联了哪个个人档案版本
- 岗位 JD、投递链接、来源和备注
- 一面、二面、主管面、HR 面等流程结果
- 面试记录和面试复盘笔记
- 普通学习笔记、资料笔记、PDF/Word/TXT/Markdown 文件
- 笔试、面试、复盘、截止日期等日程安排

核心数据默认保存在本机，不需要远程服务器。只有在你主动配置并启用远程 AI Provider 时，简历草稿或低置信度字段匹配所需内容才可能发送给所选模型服务商。

## 二、安装和启动

Windows 安装包分为 `full` 和 `lite` 两种：

- `full`：附带精简 Java Runtime，适合大多数用户。
- `lite`：需要本机已配置 Java 17。

双击安装文件后，按安装向导完成安装。安装完成后，可以通过桌面快捷方式或开始菜单打开：

```text
个人求职助手
```

软件启动后会自动打开主界面，普通使用者通常不需要手动启动数据库、后端服务或前端页面。

如果客户端打不开，请查看本说明末尾的“故障排查”。桌面端默认只在本机 `127.0.0.1:18080` 启动服务。

## 三、首次使用建议

第一次打开后，建议先进入左侧菜单：

```text
系统设置
```

设置三个保存目录：

- 简历保存目录
- 面试笔记保存目录
- 普通笔记保存目录

还可以设置：

- 简历所有者姓名、学校和毕业届别
- 默认简历命名模板
- 可复用的自定义命名模板库

建议放在容易备份的位置，例如：

```text
D:\求职材料\resumes
D:\求职材料\interview_notes
D:\求职材料\notes
```

如果不设置，软件会使用默认目录保存数据和文件。修改保存目录只影响之后新上传或新创建的文件，不会自动移动旧文件。

随后建议按顺序完成：

1. 在 `个人档案` 中建立默认档案，填写常用资料。
2. 在 `简历管理` 中上传不同版本的简历。
3. 新增岗位时，同时绑定实际使用的简历和档案版本。
4. 如需网页辅助填写，再安装并配对 ApplyMate。

## 四、主界面说明

左侧是功能菜单：

- `Dashboard`：查看整体求职数据
- `投递管理`：管理岗位投递记录
- `简历管理`：上传和维护简历文件
- `笔记管理`：管理普通笔记和资料文件
- `个人档案`：维护档案版本、历史快照和 ApplyMate 扩展授权
- `日程管理`：管理笔试、面试、复盘、截止日期等事项
- `系统设置`：设置文件保存位置和简历命名模板

右侧是当前页面内容。

## 五、Dashboard 使用说明

Dashboard 是总览页，可以快速查看：

- 总投递数
- 面试数量
- Offer 数量
- 投递状态分布
- 每周投递趋势
- 公司投递统计
- 今日日程
- 最近投递记录

如果存在未完成日程，软件顶部会显示未完成预警。点击预警可以进入日程管理并查看未完成事项。

## 六、投递管理

进入：

```text
投递管理
```

可以新增、编辑、删除岗位投递记录。

### 1. 新增投递

点击 `新增`，填写信息：

- 公司名称
- 岗位名称
- 岗位类别
- 投递批次
- 简历类别
- 当前状态
- 绑定简历
- 个人档案版本
- 投递时间
- 来源、链接、地点、薪资
- 岗位 JD
- 备注

岗位类别、简历类别、投递批次和状态都支持选择，也支持自定义。

绑定简历时可以选择已有简历，也可以临时上传一份新简历。临时上传后，系统会自动把这份简历绑定到当前投递记录。

简历文件与个人档案版本是两个独立选择：简历用于保留当时实际提交的文件；档案版本可供 ApplyMate 在该招聘页面中优先读取。若岗位没有绑定档案，ApplyMate 会使用扩展中手动选择的版本。

### 2. 查询和排序

支持按以下条件查询：

- 公司名
- 投递状态
- 岗位类别
- 投递批次
- 简历类别
- 投递时间范围

支持排序：

- 投递时间倒序
- 投递时间正序
- 公司名 A-Z
- 公司名 Z-A

也可以开启 `按公司合并`，把同一公司的多个岗位折叠到一起查看。

## 七、投递详情

在投递列表中点击某条岗位记录，可以进入详情页。

详情页可以查看和维护：

- 岗位基础信息
- 绑定简历
- 岗位 JD 预览
- 投递进度时间轴
- 自定义招聘流程
- 面试记录
- Markdown 面试笔记
- 关联日程

详情页中的投递进度、岗位 JD、关联日程、面试流程和面试笔记都可以收起或展开。信息很多时，可以只展开当前需要查看的区域。

### 1. 管理投递进度

点击时间轴中的某个流程节点，编辑区域会直接出现在该节点旁边。

可以设置：

- 当前进度
- 结果：进行中、成功、失败
- 操作时间

保存后，顶部状态和时间轴会同步更新。

每个流程节点都会保存自己的操作时间。系统会校验普通流程节点的时间顺序，后一个节点的时间不能早于前一个节点。

如果某个普通环节选择 `失败`，系统会把最终结果视为 `淘汰`。`Offer` 和 `淘汰` 是最终结果，不作为普通流程节点互相比时间。

### 2. 自定义招聘流程

不同公司流程可能不同，可以点击 `编辑流程` 自定义阶段，例如：

```text
简历筛选
笔试
一面
二面
主管面
HR 面
Offer
淘汰
```

自定义后，岗位状态会从该岗位自己的流程中选择，避免状态和进度条不一致。

### 3. 关联日程

投递详情页会展示与当前投递记录绑定的日程，例如笔试、面试、复盘或截止事项。

关联原则是：只有在日程中明确选择了当前投递记录，才会出现在该投递详情页。系统不会仅凭公司名或岗位名模糊匹配，避免误关联。

点击 `查看日程` 可以跳转到日程管理，并查看该投递相关的日程列表。

### 4. 面试记录

在 `面试流程` 区域可以新增面试记录，记录：

- 面试轮次
- 面试时间
- 面试官
- 面试问题
- 面试结果
- 难度
- 总结

### 5. 面试笔记

在 `Markdown 面试笔记` 区域可以新增面试复盘笔记。

支持：

- 新增
- 预览
- 编辑
- 下载
- 删除

面试笔记和普通笔记是分开保存的。

## 八、简历管理

进入：

```text
简历管理
```

可以管理不同版本的简历。

支持：

- 上传简历
- 编辑版本名称和备注
- PDF 简历预览
- 下载简历
- 删除简历
- 查看绑定数量
- 查看绑定了哪些投递记录
- 在本地提取简历文本
- 使用可选 AI Provider 生成可核对的档案草稿

建议给简历起清晰名称，例如：

```text
Java 后端简历
AI 工程简历
国企通用简历
银行金融科技简历
```

如果某份简历已经绑定投递记录，删除前系统会提示绑定数量。建议先确认是否仍然需要保留这份简历。

### 1. 本地文本提取

点击简历的解析操作后，系统会先在本地提取文本并显示解析结果。这个步骤不会覆盖个人档案，也不会自动调用云端 AI。

如果提取文本过少，系统会提示可能是扫描版或复杂排版 PDF。当前版本尚未实现视觉简历解析，此时可以更换可提取文本的文件，或手工维护个人档案。

### 2. 可选 AI 档案草稿

仅当已经配置并启用可用的 AI Provider 时，才可以选择模型生成结构化档案草稿。生成后可以：

- 查看格式化内容或复制 JSON。
- 合并到已有档案：保留目标档案非空内容，补充空字段和新增经历。
- 新建独立档案：创建可单独切换的档案，不修改当前档案。

AI 草稿不会静默覆盖个人档案，必须由你检查并确认导入。

## 九、个人档案

个人档案是简历解析和 ApplyMate 填写的信息源，可以保存基本信息、教育经历、实习经历、项目经历、荣誉与竞赛获奖。

### 1. 多档案版本

可以维护默认档案和多个独立版本，例如“通用档案”“前端岗位版”“国企版”。支持：

- 从空白或已有档案创建新版本。
- 编辑名称与版本说明。
- 从 JSON 导入，并选择合并现有档案或新建独立档案。
- 删除非默认档案。

投递记录可以绑定其中一个版本。ApplyMate 会优先使用当前招聘页面所关联投递的档案；没有关联时使用扩展中选择的版本。

### 2. 修订与快照

保存档案时会创建修订快照。快照可以展开查看和恢复；恢复操作会形成新的修订，不会直接抹掉当前历史。

### 3. 扩展授权

个人档案页的 `浏览器扩展` 卡片会显示待批准请求和已授权扩展。只批准自己刚在 ApplyMate 中发起的请求。授权可以随时移除，移除后扩展需要重新配对。

### 4. 桌面端与 Web 数据不互通

桌面端使用本地 H2；开发用 Web 后端通常使用 MySQL。两者默认是独立数据源，档案、快照和扩展配对不会自动同步。

普通用户应让 ApplyMate 连接 `桌面端服务 · 18080`。只有明确使用开发环境时，才选择 `Web 后端 · 8080`。

## 十、ApplyMate 浏览器扩展

ApplyMate 当前是 Alpha 阶段的 Chrome Manifest V3 扩展，需要通过开发者模式加载。

### 1. 安装扩展

1. 解压 GitHub Actions 提供的 `ApplyMate-<version>-chrome-mv3.zip`；从源码构建时则使用 `extension/.output/chrome-mv3` 目录。
2. 打开 `chrome://extensions` 或 `edge://extensions`。
3. 开启 `开发者模式`。
4. 点击 `加载已解压的扩展程序`，选择上述构建目录。

扩展更新后，需要在扩展管理页重新加载，并刷新已经打开的招聘页面。

### 2. 与 JobTracker Desktop 配对

1. 启动 JobTracker Desktop。
2. 打开 ApplyMate，选择 `自动选择` 或 `桌面端服务 · 18080`。
3. 点击 `创建配对请求`。
4. 回到 JobTracker Desktop 的 `个人档案` 页面，核对并批准刚发起的请求。
5. 返回扩展，点击 `完成配对`。
6. 选择要使用的个人档案版本。

不要批准来源不明、时间不对应或并非自己刚刚发起的请求。

### 3. 扫描与填写

打开招聘表单页面后：

1. 确认本地服务正常、档案版本正确。
2. 点击 `AI 解析页面并生成填写计划`。
3. 等待页面扫描、字段匹配和档案读取完成。
4. 展开结果，检查候选值和需要复核的字段。
5. 确认无误后，点击 `确认填写`。
6. 在招聘页面逐项复核，再由你本人提交。

扫描任务在扩展弹窗关闭后仍可继续，重新打开即可查看进度或停止任务。

当前只扫描可见且可编辑的 `input`、`textarea` 和 `select`。文件上传、密码、隐藏字段、复选框、单选框和验证码不会进入填写流程。

ApplyMate 写入后会回读验证。网页重新渲染、选项不匹配或实际值不同的项目会标记为需要复核；扩展不会假装填写成功，也不会点击最终提交按钮。

### 4. 提交后更新进度

当前版本不会自动读取招聘网站的提交结果。由你本人提交后，请回到岗位详情手动更新状态、流程时间和下一步日程。

## 十一、笔记管理

进入：

```text
笔记管理
```

这里像一个简单的文件管理器，用于管理普通笔记和资料文件。

### 1. 支持的文件类型

| 文件类型 | 能否预览 | 能否编辑内容 |
| --- | --- | --- |
| Markdown：`.md` / `.markdown` | 可以 | 可以 |
| 文本：`.txt` | 可以 | 可以 |
| PDF：`.pdf` | 可以 | 不可以 |
| Word：`.doc` / `.docx` | 可以预览提取文本 | 不可以 |

PDF 和 Word 不能直接编辑正文内容，但可以编辑文件信息，例如名称和所在位置。

### 2. 新建文件夹

点击 `新建文件夹`，可以选择：

- 文件夹名称
- 所在位置
- 本地保存位置

如果给文件夹设置了本地保存位置，放入该文件夹的新笔记会保存到对应目录。

### 3. 新建或上传笔记

可以点击：

- `新建笔记`
- `上传笔记`

上传笔记支持 Markdown、TXT、PDF、Word 文件。

### 4. 预览和编辑

点击左侧文件后，右侧默认直接显示预览内容。

如果是 Markdown 或 TXT，可以点击 `编辑内容` 修改正文。

如果需要修改名称、位置等信息，点击 `文件信息`。

### 5. 移动文件和文件夹

支持：

- 拖拽笔记到文件夹
- 拖拽文件夹到其他文件夹
- 点击 `移到根目录`
- 拖拽到左侧 `移出到根目录` 区域

移动后，左侧文件树会同步更新嵌套关系。

## 十二、日程管理

进入：

```text
日程管理
```

可以管理笔试、面试、复盘、材料截止日期、HR 跟进等事项。

日程支持：

- 新增
- 查看
- 编辑
- 标记完成
- 取消
- 删除

可以填写：

- 标题
- 类型
- 开始时间
- 结束时间
- 优先级
- 重要程度
- 关联投递
- 备注

### 1. 月视图、周视图、日视图和列表视图

日程管理支持四种视图：

- `月视图`：适合查看整个月的安排。
- `周视图`：适合查看本周每天的事项。
- `日视图`：适合查看某一天的详细安排。
- `列表视图`：适合集中查看全部日程。

顶部可以自选月份、周或日期，也可以点击 `今天` 快速回到当前日期。

### 2. 快速查看统计

日程页顶部有统计卡片：

- 今日
- 本周
- 本月
- 未完成
- 紧急

点击这些卡片可以查看对应范围或类型的日程列表。

### 3. 未完成预警

只要存在 `未完成` 日程，顶部会显示未完成预警。

`已完成` 和 `已取消` 的日程不会计入未完成预警。

## 十三、系统设置与简历命名

系统设置除三个文件保存目录外，还可以维护姓名、学校和毕业届别，并选择默认简历命名模板。

自定义模板库支持新建、编辑、复制和删除模板。模板可以组合姓名、公司、岗位、学校、届别、岗位类别、招聘批次、简历类别和地点等字段，页面会实时预览最终文件名。保存后的模板可以在新增岗位、临时上传简历时使用。

自定义模板不能直接作为全局默认模板时，系统会自动回退到通用模板并给出提示。

## 十四、数据、AI 与安全边界

### 1. 本地数据与备份

桌面端数据库和日志由 Electron 写入 Windows 应用数据目录。准确路径可能随系统用户名、安装环境和 Electron 的 `userData` 目录规则变化，不要只依赖固定路径猜测。

通常需要备份：

```text
jobtracker.mv.db
resumes\
interview_notes\
notes\
storage.properties
```

如果你在系统设置里改过保存目录，也要备份那些自定义目录。

建议定期备份整个数据目录，尤其是在重装系统或更换电脑前。

### 2. 默认本地行为

岗位、档案、快照、日程和授权记录默认保存在本地 H2 数据库，简历与笔记保存在本地文件目录。ApplyMate 通过 `127.0.0.1` 与桌面端通信，并按页面实际识别出的字段请求授权档案值，而不是读取全部 JobTracker 数据。

密码、验证码、文件上传、复选框、单选框和最终提交不在当前处理范围。ApplyMate 也不会读取面试笔记、普通笔记或日程内容。

### 3. 启用云端 AI 时

核心岗位管理、档案存储和本地规则匹配不依赖云端 AI。如果你主动配置并启用 Qwen、DeepSeek 等远程 Provider：

- 简历草稿生成可能发送提取出的简历文本。
- 低置信度字段匹配可能发送完成映射所需的页面字段上下文。
- 数据会由所选模型服务商按其条款处理。

API Key 从本机环境变量读取，不写入档案快照，也不会返回浏览器扩展。若不希望任务内容离开本机，请不要启用远程 Provider；可以只使用本地规则，或配置本地兼容模型。

## 十五、换电脑怎么办

迁移数据可以按以下步骤：

1. 在旧电脑关闭客户端。
2. 备份旧电脑的数据目录。
3. 在新电脑安装客户端。
4. 关闭新电脑客户端。
5. 将旧电脑数据复制到新电脑对应目录。
6. 如果设置过自定义保存目录，也一起复制。
7. 重新打开客户端检查数据。

ApplyMate 授权不建议直接跨设备复制。迁移完成后，请在新电脑重新配对。

## 十六、常见问题与故障排查

### 1. 修改保存位置后，旧文件会自动移动吗？

不会。

保存位置只影响之后新上传或新创建的文件。旧文件不会自动迁移，避免误移动你的资料。

### 2. Word 为什么不能像 Word 软件一样编辑？

客户端只做 Word 内容预览和文件管理，不提供 Word 原格式编辑。

如需编辑 Word 内容，请下载后用 Microsoft Word 或 WPS 修改，再重新上传。

### 3. PDF 为什么不能编辑？

PDF 是固定版式文件，客户端提供预览和下载，不直接编辑 PDF 正文。

如需修改 PDF，请使用专业 PDF 编辑工具后重新上传。

### 4. 数据会上传到服务器吗？

默认不会。

当前桌面版默认本地运行，数据保存在本机数据库和本地文件目录中。如果你主动启用远程 AI Provider，完成简历草稿或字段映射所需的内容可能发送给所选服务商，具体以“数据、AI 与安全边界”为准。

### 5. 为什么投递详情里没有显示某个日程？

只有在日程中明确选择了对应投递记录，该日程才会出现在投递详情页。仅标题里写了公司名或岗位名，不会自动关联。

### 6. ApplyMate 会自动投递吗？

不会。ApplyMate 只负责扫描、匹配、生成填写方案、确认后写入和验证。最终申请始终由你本人提交。

### 7. 不配置 AI 还能使用 ApplyMate 吗？

可以。岗位、档案、日程和本地规则匹配不依赖云端 AI。AI 主要用于可选的简历草稿和低置信度字段映射。

### 8. ApplyMate 为什么提示本地服务未启动？

- 确认 JobTracker Desktop 已进入主界面。
- 普通用户选择 `自动选择` 或 `桌面端服务 · 18080`。
- 开发环境才使用 `Web 后端 · 8080`。
- 如果两个服务同时运行，请确认扩展选中了保存目标档案和配对记录的那个服务。

### 9. 为什么无法扫描招聘页面？

- 如果刚更新扩展，先在扩展管理页重新加载，再刷新招聘页面。
- 确认目标控件可见且可编辑。
- 当前不支持文件、复选框、单选框、密码、隐藏字段和验证码。
- 自定义组件、iframe 或复杂招聘网站可能暂时无法识别。

### 10. 桌面端无法启动怎么办？

- `full` 安装包应使用自带的 Java Runtime。
- `lite` 安装包需要 Java 17。
- 查看应用数据目录中的 `logs/backend.log`，其中记录 Java 路径、数据目录、端口和启动错误。
- 默认端口是 `127.0.0.1:18080`；若被占用，请先关闭冲突程序。

### 11. 支持哪些招聘网站和操作系统？

当前 ApplyMate 使用通用 DOM 扫描，没有正式的网站兼容清单。标准可见表单更容易识别。桌面安装和打包流程目前面向 Windows 10 / 11，尚未提供已验证的 macOS 或 Linux 安装包。

## 十七、推荐使用习惯

建议这样使用：

1. 每投递一个岗位，及时新增记录。
2. 每条投递绑定实际使用的简历和档案版本。
3. ApplyMate 填写后逐项复核，提交后回到 JobTracker 手动更新进度。
4. 笔试、面试、复盘和截止日期都建成日程。
5. 面试后马上补充面试记录和复盘笔记。
6. AI 生成的草稿先检查，再选择合并或新建档案。
7. 只批准自己刚刚发起的扩展配对请求。
8. 每周备份一次数据库和自定义文件目录。

这样后续复盘求职进度、准备面试、整理材料都会更清晰。
