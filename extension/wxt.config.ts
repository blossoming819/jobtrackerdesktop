import { defineConfig } from 'wxt'
export default defineConfig({ manifest: { name: 'ApplyMate', description: '智能求职信息填写助手', permissions: ['activeTab', 'storage'], host_permissions: ['http://127.0.0.1/*'] } })
